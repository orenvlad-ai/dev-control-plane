# Stage 6 aggregate install and same-identity continuation contract

contract_revision: 2026-08-21.1

technical_status: ACTIVE; aggregate source merged; one governed install and same-identity continuation authorized

owner_acceptance: not requested or claimed

This contract is the bounded repository authority for the formal Stage 6
source-to-install-to-live continuation. It does not authorize Stage 7, WBC
shadow, WBC PR #987, production or cutover.

## 1. Reviewed aggregate source

The complete local seam package was published as managed-source PR #76. Its
exact reviewed identity is:

- base `11401ff6eadb80fd87e48229fb8c5458095a63b1`, tree
  `91bf6e25ec1b0e0f971ad36f7b80272aded2482c`;
- aggregate head `b0c2b6df76adf205229e49c48a1d7277aa7b5059`, tree
  `a6e3c3347bbbddd256e9edbfc541f115813249d2`;
- semantic/security review `4992765757`, bound to that head, with no blocking
  finding and zero review threads;
- GitHub workflow `32477135149`: `package` and `source` succeeded on that exact
  head;
- normal merge commit `d084ae3cf0cb3e5e32ebefa197031c24a2b6392d`,
  whose tree is exactly `a6e3c3347bbbddd256e9edbfc541f115813249d2`.

The package closes the canonical prompt/spawn identity, truthful
workflow/model/runtime projection, exact native runtime adoption, technical
failure classification, fenced provider-effect reconciliation, immutable
successor Revision, result dedupe/contradiction, readmission, Admission and
Release Train proof seams. Disposable schema-85, model-free and fake-provider
tests proved restart, stale/foreign identity, drift, conflict and zero duplicate
model/provider effects. No installed, live SQLite or provider effect occurred
during source completion.

## 2. Immutable predecessor

Exactly one predecessor may be replaced:

- installed source `11401ff6eadb80fd87e48229fb8c5458095a63b1`, tree
  `91bf6e25ec1b0e0f971ad36f7b80272aded2482c`;
- receipt SHA-256
  `098056d800d41f666708b7697d6ccef9f3b5cd2e077a939d89dcf0b1f35767e2`;
- SQLite schema `85`;
- Task `dcp-v2-twin-canary-v1`, Revision
  `v2-13f81f321f99d1117dc931419e0bea3945ee35a5`, leased Command
  `v2-e028f779a18417e990911057f7db7c666f7487ca` and launching Worker Action
  `v2-40f87d048813533daa1108b4316c09139acf0a8f` with no runtime id;
- native card `1`, session `dcp-wbc-integration-lab-1`, reserved task and native
  Action `dcp-model-dcp-v2-twin-canary-v1-worker-1` queued with no slot,
  launch or runtime;
- Task/Revision/Command/Action `1/1/1/1`, Admission/Incident/ExternalEvent/
  Result `0/0/0/0`, 74 legacy model Actions and zero active.

Missing rows are not zero. Any identity, receipt, schema, cardinality, runtime,
provider or ownership contradiction stops before installation.

## 3. One aggregate installation

Only `bin/dcp-ao install-stage6-aggregate` may replace the predecessor. The old
`install-stage6-recovery` command remains historical and cannot represent this
schema-85-to-schema-85 replacement.

The aggregate installer must:

1. verify the exact predecessor bundle and receipt, schema and sole durable and
   native identities while the current app is ready;
2. prove the target lab still has its exact main, no canary branch/PR/provider
   run, and that WBC PR #987 remains frozen at its named head;
3. build only the source pinned by `upstream/dcp-orchestrator.lock`;
4. repeat the complete preflight under the canonical gateway singleton;
5. prove the exact app/daemon pair and zero model process, request only the
   canonical graceful app quit, then prove a fully stopped contour;
6. recheck the exact schema-85 identity after stop, make verified recoverable
   bundle/state/data/receipt backups and atomically replace only the app bundle;
7. write and verify a new exact receipt, preserve schema 85 and every
   predecessor identity/count, and emit one strict lower-camel aggregate
   response;
8. automatically restore and prove the predecessor bundle, data and receipt if
   install or stopped-preflight verification fails.

No migration, checkpoint, direct SQLite write, blind kill, second installation
attempt or automatic retry is authorized. `bin/dcp-ao
preflight-stage6-aggregate` is the exact stopped post-install verifier.

## 4. Mandatory hard stop

If this one aggregate installation exposes another same-class pre-model or
native-identity predicate, prompt/reservation disagreement, legacy
second-authority disagreement or false runtime/model projection, stop
immediately. Do not patch, open a corrective source PR, reinstall, retry start
or create a substitute identity. The legacy second-authority bridge must be
simplified or removed before any later attempt.

Ambiguous stop, backup, install, rollback or receipt proof also stops. A
technical defect is `BLOCKED` or failed, never Human Gate.

## 5. Same-identity live continuation

Only after the stopped aggregate preflight succeeds may the governed gateway
start the exact installed app once. Startup must adopt the already leased
Command and already queued native Action. Submit, replay-submit, replacement
Task/card/session, second initial Worker and manual provider/model calls are
forbidden.

The same Task may then proceed through one bounded Worker, exact-head CI, one
fresh context-free Reviewer, at most one task-level findings repair with new
CI/review, mechanical FIFO Admission and the repository-owned Release Train.
Only the Release Train may merge, build, deploy and publish immutable Result
proof. A deployable terminal result requires exact merge/artifact/deployed SHA,
canonical health and provenance. Duplicate or out-of-order events, restart and
provider readback must not duplicate model, merge or deploy effects.

Stage 7 adversarial qualification is not deliberately started by this
continuation.

## 6. Frozen surfaces

- no manual target edit, push, merge, deploy, SSH or service-manager action;
- no WBC/PR #987, production, business-data or credential mutation;
- no production secrets for DCP or model roles;
- no Selectel action except the exact integration-lab Release Train adapter;
- no co-tenant mutation;
- no Stage 7, WBC shadow or cutover;
- no owner acceptance inferred from technical completion.

## 7. Terminal evidence

After the one installation and live continuation reaches an unambiguous
technical `COMPLETE` or `BLOCKED`, at most one evidence-only
`dev-control-plane` PR may update current repository truth. It may record exact
receipts, identities, checks, reviews, manifests, Result and deploy proof; it
may not add correction, install or live authority.

The current entry remains the
[program manifest](DCP_WBC_INTEGRATION_TWIN_CURRENT_PROGRAM_MANIFEST.md).
